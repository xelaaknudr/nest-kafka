name: 🚀 CD (AWS) — Deploy to EKS

on:
  workflow_run:
    workflows:
      - '🔨 CD (AWS) — Build & Push Images'
    types: [completed]
    branches: [main]

concurrency:
  group: deploy-aws-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Helm upgrade → AWS EKS
    runs-on: ubuntu-latest
    environment: aws-production
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: 🔐 Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: ☸️ Get EKS kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region ${{ secrets.AWS_REGION }} \
            --name ${{ secrets.AWS_EKS_CLUSTER_NAME }}

      - name: 🪖 Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.16.0

      - name: 🔓 Unlock stuck Helm release (if pending)
        run: |
          STATUS=$(helm status roze-aws --output json 2>/dev/null | jq -r '.info.status' || echo "not-found")
          echo "Current release status: $STATUS"
          if [[ "$STATUS" == "pending-install" || "$STATUS" == "pending-upgrade" || "$STATUS" == "pending-rollback" ]]; then
            echo "Release is stuck in $STATUS, rolling back..."
            helm rollback roze-aws 0 || helm uninstall roze-aws || true
          fi

      - name: 🔍 Pre-flight checks (ESO + awssm-secret)
        run: |
          echo "=== ESO CRDs ==="
          kubectl get crd | grep external-secrets || echo "❌ ESO not installed!"

          echo "=== awssm-secret ==="
          kubectl get secret awssm-secret -n default && echo "✅ awssm-secret exists" || echo "❌ awssm-secret MISSING"

          echo "=== ClusterSecretStore ==="
          kubectl get clustersecretstore 2>/dev/null || echo "❌ No ClusterSecretStore found"

      - name: 🚀 Helm upgrade — deploy all services
        run: |
          helm upgrade --install roze-aws ./k8s/roze \
            --set global.cloudProvider=aws \
            --set reservations.image.repository=${{ secrets.AWS_ECR_REGISTRY }}/roze/reservation \
            --set reservations.image.tag=${{ github.event.workflow_run.head_sha }} \
            --set auth.image.repository=${{ secrets.AWS_ECR_REGISTRY }}/roze/auth \
            --set auth.image.tag=${{ github.event.workflow_run.head_sha }} \
            --set notifications.image.repository=${{ secrets.AWS_ECR_REGISTRY }}/roze/notifications \
            --set notifications.image.tag=${{ github.event.workflow_run.head_sha }} \
            --set payments.image.repository=${{ secrets.AWS_ECR_REGISTRY }}/roze/payments \
            --set payments.image.tag=${{ github.event.workflow_run.head_sha }} \
            --debug \
            --timeout 15m

      - name: 🩺 Diagnostics on failure
        if: failure()
        run: |
          echo "=== Pods ==="
          kubectl get pods -n default -o wide

          echo "=== Events ==="
          kubectl get events -n default --sort-by='.lastTimestamp' | tail -30

          echo "=== Auth pod describe ==="
          kubectl describe pod -l app=auth -n default || true

          echo "=== ExternalSecrets ==="
          kubectl get externalsecret -n default || true
          kubectl describe externalsecret auth-secret -n default || true

      - name: ✅ Verify rollout — all services
        run: |
          kubectl rollout status deployment/reservations --timeout=5m
          kubectl rollout status deployment/auth --timeout=5m
          kubectl rollout status deployment/notifications --timeout=5m
          kubectl rollout status deployment/payments --timeout=5m
