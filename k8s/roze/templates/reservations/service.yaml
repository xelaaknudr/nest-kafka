apiVersion: v1
kind: Service

metadata:
  name: reservations
  labels:
    app: reservations

spec:
  type: NodePort

  selector:
    app: reservations

  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000