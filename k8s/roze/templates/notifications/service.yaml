apiVersion: v1
kind: Service
metadata:
  name: notifications
  labels:
    app: notifications

spec:
  type: ClusterIP

  selector:
    app: notifications

  ports:

    - name: tcp
      protocol: TCP
      port: 3004
      targetPort: 3004