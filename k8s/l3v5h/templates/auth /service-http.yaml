apiVersion: v1
kind: Service

metadata:
  name: auth-http
  labels:
    app: auth-http

spec:
  type: NodePort
  selector:
    app: auth
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001