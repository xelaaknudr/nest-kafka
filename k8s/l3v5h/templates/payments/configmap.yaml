apiVersion: v1
kind: ConfigMap

metadata:

  name: payments-config


data:

  NOTIFICATIONS_HOST: notifications

  NOTIFICATIONS_PORT: "3004"

  TEST: "200"
  RABBITMQ_URI: "amqp://rabbitmq:rabbitmq@l3v5h-rabbitmq:5672"
